import { Container } from "react-bootstrap";
import CheckoutForm from "./CheckoutForm";

const Checkout = () => {
	return (
		<Container>
			<h1>Checkout</h1>
			<CheckoutForm />
		</Container>
	);
};

export default Checkout;
